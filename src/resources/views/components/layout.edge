<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    <title>
      {{ title || 'Mon App Flashcards' }}
    </title>
    @stack('dumper')
  </head>
  <body>
    @include('partials/nav')

    <main>
      {{{ await $slots.main() }}}
    </main>

    {{-- @include('partials/footer') --}}
  </body>
</html>
